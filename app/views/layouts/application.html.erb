<!DOCTYPE html>
<html>
  <head>
    <title>Blog da Blogaira</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="bg-primary text-white mb-4 p-4 text-center">
      <%= link_to root_path, class: 'text-white text-decoration-none' do %>
        <h1 class='display-4'>Blog da Blogaira</h1>
      <% end %>
      <p class="lead">
        Um blog de blogueiragens de blogairas
      </p>
    </div>
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success'%> 
					alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <div class="row">
        <div class="col-4 offset-8">
          <div class="search">
            <%= form_with url: search_posts_path, method: :get, local: true, class: 'form-inline' do |form| %>
              <div class="form-group mx-3 mb-3 d-flex align-items-center">
                <%= form.search_field :q, class: 'form-control mx-2', required: true, placeholder: 'Busca', value: params[:q] %>
                <%= form.submit 'Buscar', class: 'btn btn-outline-primary btn-sm', data: {disable_with: 'Buscando...'} %>
              </div>
              
            <% end %>
          </div>
        </div>
      </div>

      <%= yield %>
    </div>

    <footer class='bg-primary text-white text-center mt-5 p-4'>
        Copyright ₢ Luiza Carvalho <%= Date.current.year %>
    </footer>
    
  </body>
</html>
