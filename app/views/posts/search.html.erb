<h2 class="text-muted" >
  <% if @posts.any? %>
    <%= pluralize(@posts.count, 'resultado' )%> encontrados:
  <% else %>
    Nenhum resultado encontrado.
  <% end %>
</h2>

<%= render @posts %>